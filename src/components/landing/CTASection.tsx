import type { CTASectionProps } from "@/lib/types";

export function CTASection({ content, className }: CTASectionProps) {
  return (
    <section className={className}>
      {/* CTA section implementation will be added in task 3.5 */}
    </section>
  );
}