import type { HeroSectionProps } from "@/lib/types";

export function HeroSection({ content, className }: HeroSectionProps) {
  return (
    <section className={className}>
      {/* Hero section implementation will be added in task 3.1 */}
    </section>
  );
}