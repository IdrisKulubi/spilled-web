import type { HeaderProps } from "@/lib/types";

export function Header({ className }: HeaderProps) {
  return (
    <header className={className}>
      {/* Header implementation will be added in task 2.1 */}
    </header>
  );
}