import type { FooterProps } from "@/lib/types";

export function Footer({ className }: FooterProps) {
  return (
    <footer className={className}>
      {/* Footer implementation will be added in task 2.2 */}
    </footer>
  );
}